<DataAnalysisApp>:
    orientation: "vertical"
    FileChooserListView:
        on_selection: root.load_data(self.path, self.selection)

    BoxLayout:
        id: graph_container
        size_hint_y: 0.7
